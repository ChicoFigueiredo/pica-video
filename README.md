# Pica vídeos

```bash
    sudo apt install ffmpeg
    pip3 install --pre torch torchvision torchaudio --index-url https://download.pytorch.org/whl/nightly/cu121    
    pip install tqdm faster-whisper transformers sentencepiece 
    pip install ttkthemes

    python -O ~/dev/python/pica.video/pica.video.07.py --recursivo --skip-prontos "*.mp4"        
    
    
    #python -O ~/dev/python/pica.video/pica.video.07.py --recursivo --skip-prontos "*.mp4" --idioma-original en        
```


# Traduz TXT

```bash
    #pip install deep-translator
    #pip install googletrans==4.0.0-rc1
    pip install translate tqdm

```